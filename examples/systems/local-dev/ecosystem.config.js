// Optional PM2-native escape hatch.
// LocalLink reads service topology from locallink.services.yml first and can
// derive PM2 launch commands from each service blueprint's CMD.
module.exports = {
  apps: [],
};
